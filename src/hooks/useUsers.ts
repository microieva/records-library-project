export {};
/*import { useEffect } from 'react';
import { useDispatch } from 'react-redux'

import { fetchData } from '../redux/actions/users'

export const useUsers = () => {
  const dispatch = useDispatch()
  
  useEffect(() => {
    dispatch(fetchData());
  }, [dispatch]);
  
}*/
